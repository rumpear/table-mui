import makeStyles from '@mui/styles/makeStyles';

export const useStyles = makeStyles({
  btnWrapper: {
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
    gap: 10,
  },
  notification: {
    fontSize: '25px',
    textAlign: 'center',
  },
});
