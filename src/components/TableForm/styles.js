import makeStyles from '@mui/styles/makeStyles';

export const useStyles = makeStyles({
  button: {
    marginTop: '10px',
    maxWidth: '90px',
  },
  form: {
    display: 'flex',
    flexDirection: 'column',
    alignItems: 'center',
  },
  formControl: {
    width: '100%',
  },
  input: {
    width: '100%',
    margin: 10,
  },
});
