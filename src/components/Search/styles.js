import makeStyles from '@mui/styles/makeStyles';

export const useStyles = makeStyles({
  form: {
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    gap: 10,
    marginBottom: 15,
    // width: 400,
  },
});
